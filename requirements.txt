# ==========================================
# 🚗 车载智能 RAG 问答助手依赖清单
# ==========================================

# --- 核心大模型框架 (LangChain 生态) ---
# 建议锁定大版本号，因为 LangChain 更新频繁，容易出现 API 不兼容
langchain>=0.2.0
langchain-community>=0.2.0
langchain-core>=0.2.0
langchain-text-splitters>=0.2.0

# --- 向量模型与重排模型 ---
# 用于加载 BGE Embedding 和 BGE Reranker
sentence-transformers>=2.7.0
langchain-huggingface>=0.0.3

# --- 智谱大模型接入 ---
# 官方 SDK，用于调用 GLM-4-Flash
zhipuai>=2.0.1

# --- 向量数据库 ---
# 注意：官方推荐在 0.2.x 版本后使用 langchain-chroma 独立包
chromadb>=0.5.0
langchain-chroma>=0.1.2

# --- 多路召回组件 (词频与分词) ---
rank_bm25==0.2.2
jieba==0.42.1

# --- 文档解析 ---
# PyMuPDF 是目前解析 PDF 速度和精度平衡得最好的库之一
pymupdf>=1.24.0

# --- 后端微服务框架 ---
fastapi>=0.100.0
uvicorn>=0.23.0
pydantic>=2.0.0
python-multipart>=0.0.9  # 用于处理 FastAPI 的文件上传

# --- 前端交互界面 ---
streamlit>=1.30.0
requests>=2.31.0

# --- 环境变量管理 ---
python-dotenv>=1.0.0